% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{imapply}
\alias{imapply}
\alias{voxelApply}
\alias{lineApply}
\alias{sliceApply}
\title{Apply a function over an image}
\usage{
imapply(x, margin, fun, ..., simplify = TRUE, threads = NULL)

voxelApply(x, fun, ..., simplify = TRUE, threads = NULL)

lineApply(x, fun, ..., axis = 1L, simplify = TRUE, threads = NULL)

sliceApply(x, fun, ..., axis = 3L, simplify = TRUE, threads = NULL)
}
\arguments{
\item{x}{An image, or any array. No custom class is required.}

\item{margin}{The dimensions to retain, as for \code{\link[base:apply]{base::apply()}}.}

\item{fun}{A function to apply.}

\item{...}{Further arguments to \code{fun}.}

\item{simplify}{Whether to simplify the result to an array where possible.}

\item{threads}{Number of threads to use, or \code{NULL} to consult
\code{getOption("imply.threads")}. See \link{parallelism}.}

\item{axis}{For \code{lineApply()}, the axis lines run along; for \code{sliceApply()},
the axis slices are cut across.}
}
\value{
For \code{imapply()}, as \code{\link[base:apply]{base::apply()}}. For \code{voxelApply()}, an image
when the function returns a single value per location, otherwise an array.
}
\description{
\code{imapply()} is a memory-efficient analogue of \code{\link[base:apply]{base::apply()}}. It gives the
same answer, but where \code{apply()} permutes the whole array into a fresh copy
before looping, \code{imapply()} gathers each sub-array directly through the
stride vector. Peak memory is therefore the input plus the result, rather
than twice the input plus the result, which matters once an image is larger
than a comfortable fraction of memory.
}
\details{
The remaining functions differ only in how much of the \emph{space} they hand to
\code{fun} at a time: a single location for \code{voxelApply()}, a one-dimensional
line for \code{lineApply()}, and a two-dimensional slice for \code{sliceApply()}.

In every case the values held at each location travel with the unit. So for
an image with a time series at each location, \code{voxelApply()} passes one
series, \code{lineApply()} passes a line's worth of series, and \code{sliceApply()}
passes a slice's worth. This is forced for \code{voxelApply()}, since a location
on its own carries nothing to compute with, and the other two follow it so
that the three read as one progression.

\preformatted{
  dim(x) = 4 x 5 x 6 x 10, spatial = 3

  voxelApply(x, f)           f sees  10
  lineApply(x, f, axis = 1)  f sees  4 x 10
  sliceApply(x, f, axis = 3) f sees  4 x 5 x 10
}

\code{axis} always names a spatial dimension, but means what the name of each
function implies: a line \emph{runs along} its axis, whereas a slice is \emph{cut
across} its axis. Use \code{\link[=imapply]{imapply()}} directly to iterate over a non-spatial
dimension, such as applying a function to each volume in a time series.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti.R
\name{interop}
\alias{interop}
\alias{fromNifti}
\alias{toNifti}
\alias{fromMriImage}
\alias{toMriImage}
\title{Interoperability with other image classes}
\usage{
fromNifti(x, ...)

toNifti(x, datatype = "auto")

fromMriImage(x, ...)

toMriImage(x)
}
\arguments{
\item{x}{An image to convert.}

\item{...}{Further arguments to \code{denseImage()}.}

\item{datatype}{The NIfTI datatype to write, or \code{"auto"}.}
}
\value{
\code{fromNifti()} and \code{fromMriImage()} return a \code{denseImage}.
\code{toNifti()} returns an object of class \code{niftiImage}, and \code{toMriImage()}
an \code{MriImage}.
}
\description{
\code{imply} deliberately knows nothing about file formats: an image's geometry
is a 4x4 affine like any other. These bridges convert to and from the image
classes of packages that do handle formats, and are available whenever
those packages are installed.
}
\details{
A NIfTI image's resolved xform maps voxel to world coordinates and bakes in
voxel size, which is exactly what \code{worldTransform()} returns for an imply
image, so the conversion is a copy of the data plus a transfer of geometry
(decomposed into \code{worldTransform()}'s rotation/translation and
\code{voxelSize()}'s scale on the way in).
}

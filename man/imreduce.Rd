% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduce.R
\name{imreduce}
\alias{imreduce}
\title{Built-in reductions}
\usage{
imreduce(x, margin, what, na.rm = FALSE, threads = NULL)
}
\arguments{
\item{x}{An image, or any array.}

\item{margin}{The dimensions to retain, as for \code{\link[base:apply]{base::apply()}}.}

\item{what}{The reduction: one of \code{"sum"}, \code{"mean"}, \code{"min"}, \code{"max"},
\code{"range"}, \code{"prod"}, \code{"var"}, \code{"sd"}, \code{"which.min"}, \code{"which.max"},
\code{"any"}, \code{"all"} or \code{"countNA"}.}

\item{na.rm}{Whether to ignore missing values.}

\item{threads}{Number of threads, or \code{NULL} for the default. See
\link{parallelism}.}
}
\value{
A vector or array, shaped over \code{margin} as \code{\link[base:apply]{base::apply()}} would
shape it. \code{"range"} yields two values per call, so it gains a leading
dimension.
}
\description{
\code{imreduce()} computes a summary over the margins of an image without
calling back into R for each one. That matters for two reasons: there is no
per-call interpreter overhead, and (since nothing touches R) the loop can
run on worker threads. \code{imapply()} recognises the equivalent base functions
and routes calls here automatically where the answer has been shown to be
the same.
}
\details{
Accumulation is done in double mode whatever the values are stored as, so
the answer does not depend on the storage type and error does not compound
with the number of values.

Note that for the arithmetic reductions this may not agree with the base
equivalent to the last bit: R accumulates \code{sum()} and \code{mean()} in long
double, which on some platforms is wider than double. Differences are of
the order of \code{1e-16}. \code{imapply()} therefore routes automatically only where
the answers are provably identical (\code{min}, \code{max}, \code{range}, \code{which.min} and
\code{which.max}) and leaves the rest to be asked for deliberately.

\code{"var"} is the variance of the values, as \code{var(as.vector(x))} would give.
It is not \code{\link[stats:var]{stats::var()}} applied to a sub-array, which for a matrix returns
a covariance matrix.
}

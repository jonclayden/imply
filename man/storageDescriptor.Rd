% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec.R
\name{storageDescriptor}
\alias{storageDescriptor}
\title{Storage descriptors}
\usage{
storageDescriptor(
  type,
  endian = "little",
  offset = 0,
  slope = NULL,
  intercept = NULL,
  layout = NULL,
  bitOrder = "lsb"
)
}
\arguments{
\item{type}{The stored data type, as above.}

\item{endian}{The byte order of the stream: \code{"little"} or \code{"big"}.}

\item{offset}{The position of the first value, in bytes from the start of
the stream.}

\item{slope, intercept}{Scaling, as \code{value = stored * slope + intercept}.
When reading, \code{NULL} means no scaling. When writing, \code{NULL} means that
a scaling is chosen to use the whole range of an integer type, if the
data need one.}

\item{layout}{How the image's axes map onto the order of values in the
stream, as described for \code{\link[=storageLayout]{storageLayout()}}. \code{NULL} means the stream
holds the image in its own axis order, first axis fastest.}

\item{bitOrder}{For \code{"bit"} data, whether the first value of each byte is
its least or most significant bit.}
}
\value{
An object of S7 class \code{storageDescriptor}.
}
\description{
A storage descriptor says how the values of an image sit in a stream of
bytes: their type, byte order and position, any scaling applied to them, and
the order of the image's axes in the stream. It deliberately says nothing
about any particular file format. A format package parses a header into a
descriptor, and \code{\link[=readImageData]{readImageData()}} and \code{\link[=writeImageData]{writeImageData()}} do the rest.
}
\details{
Types are named by size and signedness: \code{"int8"}, \code{"uint8"}, \code{"int16"},
\code{"uint16"}, \code{"int32"}, \code{"uint32"}, \code{"int64"}, \code{"float32"}, \code{"float64"},
\code{"complex64"} and \code{"complex128"}, plus \code{"rgb24"} and \code{"rgba32"} for colour
and \code{"bit"} for one bit per value. The C-style names \code{"char"}, \code{"int"},
\code{"float"} and \code{"double"} are accepted too, as \code{"int8"}, \code{"int32"},
\code{"float32"} and \code{"float64"} respectively. C leaves the signedness of \code{char}
to the platform, so it is fixed as signed here; use \code{"uint8"} for unsigned
bytes.

When read, integer types of up to 32 bits become R integers unless scaled,
and wider integers, floating-point types and scaled values become doubles.
64-bit integers beyond 2^53 cannot be held exactly as doubles and are
refused. Colour values become integers from 0 to 255, with an extra
trailing dimension of three or four channels. Bits become logical values.
Scaling is not applied to complex or colour data.
}
\examples{
storageDescriptor("int16", endian = "big", offset = 352)
}
